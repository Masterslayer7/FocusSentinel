{
  "name": "focussentinel",
  "version": "0.1.0",
  "description": "Privacy-first, local-only digital study evaluator",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "electron": "^30.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
